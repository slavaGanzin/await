# await — Project Notes

## README is auto-generated

**Do NOT edit README.md directly.** It is regenerated by `hooks/pre-commit` on every commit.

To update README: edit `hooks/pre-commit`, then commit anything — the hook rebuilds await, runs `./build/await --help`, and writes README.md automatically.

To regenerate manually without committing:
```bash
bash hooks/pre-commit
```

## Build

```bash
cmake -B build/ && cmake --build build/
```

Binary lands at `build/await`. Tests expect `./await` in the project root — copy it after building:
```bash
cp build/await ./await
```

## Tests

```bash
cd tests && pytest test_await.py -v
```

## Version

Version is hardcoded in `await.c` — search for `printf("2.x.x\n")` in the `case 'v':` handler.
